---
import Layout from '../layouts/Layout.astro';
import { getCollection } from 'astro:content';

// Get recent records for preview
const allRecords = await getCollection('records');
const recentRecords = allRecords
  .sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf())
  .slice(0, 3);
---

<Layout title="主页 - 我的个人网站" description="欢迎来到我的个人网站">
  <!-- Hero Section -->
  <section class="py-20">
    <div class="container">
      <div class="flex flex-col items-center gap-8 text-center md:flex-row md:text-left">
        <div class="relative">
          <div class="h-40 w-40 overflow-hidden rounded-full bg-gradient-to-br from-primary-400 to-primary-600 p-1">
            <div class="flex h-full w-full items-center justify-center rounded-full bg-gray-100 text-6xl dark:bg-gray-800">
              👋
            </div>
          </div>
        </div>

        <div class="flex-1">
          <h1 class="mb-4 text-4xl font-bold md:text-5xl">
            你好，我是 <span class="text-primary-600">Your Name</span>
          </h1>
          <p class="mb-6 text-xl text-gray-600 dark:text-gray-400">
            一名热爱技术的开发者。喜欢探索新技术，分享学习心得。
          </p>
          <div class="flex flex-wrap gap-3">
            <a href="/experience" class="btn btn-primary">
              查看我的经历 →
            </a>
            <a href="/records" class="btn btn-secondary">
              阅读记录
            </a>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Basic Info Section -->
  <section class="py-12">
    <div class="container">
      <h2 class="mb-8 text-center text-2xl font-bold">关于我</h2>
      <div class="grid gap-6 md:grid-cols-3">
        <div class="card text-center">
          <div class="mb-3 text-3xl">📧</div>
          <h3 class="mb-2 font-semibold">邮箱</h3>
          <p class="text-sm text-gray-600 dark:text-gray-400">
            <a href="mailto:your@email.com" class="hover:text-primary-600">
              your@email.com
            </a>
          </p>
        </div>

        <div class="card text-center">
          <div class="mb-3 text-3xl">💻</div>
          <h3 class="mb-2 font-semibold">GitHub</h3>
          <p class="text-sm text-gray-600 dark:text-gray-400">
            <a href="https://github.com/BEGINRX" target="_blank" rel="noopener" class="hover:text-primary-600">
              @BEGINRX
            </a>
          </p>
        </div>

        <div class="card text-center">
          <div class="mb-3 text-3xl">🔧</div>
          <h3 class="mb-2 font-semibold">技术栈</h3>
          <p class="text-sm text-gray-600 dark:text-gray-400">
            JavaScript, TypeScript, React, Node.js
          </p>
        </div>
      </div>
    </div>
  </section>

  <!-- Recent Records Section -->
  <section class="py-12">
    <div class="container">
      <div class="mb-8 flex items-center justify-between">
        <h2 class="text-2xl font-bold">最新记录</h2>
        <a href="/records" class="text-sm text-primary-600 hover:underline">
          查看全部 →
        </a>
      </div>

      {recentRecords.length > 0 ? (
        <div class="grid gap-6 md:grid-cols-3">
          {recentRecords.map((record) => (
            <a href={`/records/${record.slug}`} class="card block transition-transform hover:-translate-y-1">
              {record.data.image && (
                <div class="mb-4 aspect-video overflow-hidden rounded-lg bg-gray-100 dark:bg-gray-700">
                  <img
                    src={record.data.image}
                    alt={record.data.title}
                    class="h-full w-full object-cover"
                  />
                </div>
              )}
              <div class="mb-2 text-sm text-primary-600">
                {record.data.type === 'log' && '日志'}
                {record.data.type === 'doc' && '文档'}
                {record.data.type === 'quote' && '短句'}
              </div>
              <h3 class="mb-2 font-semibold">{record.data.title}</h3>
              <p class="text-sm text-gray-600 dark:text-gray-400">
                {record.data.date.toLocaleDateString('zh-CN')}
              </p>
            </a>
          ))}
        </div>
      ) : (
        <div class="card text-center text-gray-600 dark:text-gray-400">
          <p>暂无记录，敬请期待...</p>
        </div>
      )}
    </div>
  </section>
</Layout>

<style>
  section:nth-child(even) {
    @apply bg-gray-50 dark:bg-gray-800/50;
  }
</style>
